/*
 * Gerrit Code Review REST API
 * The Gerrit Code Review REST API. Paths, parameters, and payload schemas are generated statically from the server's REST bindings; human-authored summaries and descriptions are supplied here and merged in at build time.
 *
 * The version of the OpenAPI document: 3.15.0-SNAPSHOT
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package com.google.gerrit.client.model;

import java.util.Objects;
import com.google.gerrit.client.model.GroupInfo;
import java.util.List;
import java.util.Map;



import java.io.IOException;
import java.lang.reflect.Type;
import java.util.logging.Level;
import java.util.logging.Logger;
import java.util.ArrayList;
import java.util.Collections;
import java.util.HashSet;
import java.util.HashMap;
import java.util.List;
import java.util.Map;

import com.google.gson.Gson;
import com.google.gson.GsonBuilder;
import com.google.gson.JsonParseException;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.reflect.TypeToken;
import com.google.gson.JsonPrimitive;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import com.google.gson.JsonDeserializationContext;
import com.google.gson.JsonDeserializer;
import com.google.gson.JsonSerializationContext;
import com.google.gson.JsonSerializer;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.JsonArray;
import com.google.gson.JsonParseException;

import com.google.gerrit.client.JSON;

@javax.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen", comments = "Generator version: 7.25.0")
public class GetGroupsDefaultResponse extends AbstractOpenApiSchema {
    private static final Logger log = Logger.getLogger(GetGroupsDefaultResponse.class.getName());

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!GetGroupsDefaultResponse.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'GetGroupsDefaultResponse' and its subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final Type typeInstanceMapStringGroupInfo = new TypeToken<Map<String, GroupInfo>>(){}.getType();
            final TypeAdapter<Map<String, GroupInfo>> adapterMapStringGroupInfo = (TypeAdapter<Map<String, GroupInfo>>) gson.getDelegateAdapter(this, TypeToken.get(typeInstanceMapStringGroupInfo));

            final Type typeInstanceListGroupInfo = new TypeToken<List<GroupInfo>>(){}.getType();
            final TypeAdapter<List<GroupInfo>> adapterListGroupInfo = (TypeAdapter<List<GroupInfo>>) gson.getDelegateAdapter(this, TypeToken.get(typeInstanceListGroupInfo));

            return (TypeAdapter<T>) new TypeAdapter<GetGroupsDefaultResponse>() {
                @Override
                public void write(JsonWriter out, GetGroupsDefaultResponse value) throws IOException {
                    if (value == null || value.getActualInstance() == null) {
                        elementAdapter.write(out, null);
                        return;
                    }

                    // check if the actual instance is of the type `Map<String, GroupInfo>`
                    if (value.getActualInstance() instanceof Map<?, ?>) {
                    }
                    // check if the actual instance is of the type `List<GroupInfo>`
                    if (value.getActualInstance() instanceof List<?>) {
                        List<?> list = (List<?>) value.getActualInstance();
                        if (!list.isEmpty() && list.get(0) instanceof GroupInfo) {
                            JsonArray array = adapterListGroupInfo.toJsonTree((List<GroupInfo>)value.getActualInstance()).getAsJsonArray();
                            elementAdapter.write(out, array);
                            return;
                        }
                    }
                    throw new IOException("Failed to serialize as the type doesn't match oneOf schemas: List<GroupInfo>, Map<String, GroupInfo>");
                }

                @Override
                public GetGroupsDefaultResponse read(JsonReader in) throws IOException {
                    Object deserialized = null;
                    JsonElement jsonElement = elementAdapter.read(in);

                    int match = 0;
                    ArrayList<String> errorMessages = new ArrayList<>();
                    TypeAdapter actualAdapter = elementAdapter;

                    // deserialize Map<String, GroupInfo>
                    try {
                        // validate the JSON object to see if any exception is thrown
                        if (!jsonElement.isJsonObject()) {
                            throw new IllegalArgumentException(String.format(java.util.Locale.ROOT, "Expected json element to be a object type in the JSON string but got `%s`", jsonElement.toString()));
                        }

                        actualAdapter = adapterMapStringGroupInfo;
                        match++;
                        log.log(Level.FINER, "Input data matches schema 'Map<String, GroupInfo>'");
                    } catch (Exception e) {
                        // deserialization failed, continue
                        errorMessages.add(String.format(java.util.Locale.ROOT, "Deserialization for Map<String, GroupInfo> failed with `%s`.", e.getMessage()));
                        log.log(Level.FINER, "Input data does not match schema 'Map<String, GroupInfo>'", e);
                    }
                    // deserialize List<GroupInfo>
                    try {
                        // validate the JSON object to see if any exception is thrown
                        if (!jsonElement.isJsonArray()) {
                            throw new IllegalArgumentException(String.format(java.util.Locale.ROOT, "Expected json element to be a array type in the JSON string but got `%s`", jsonElement.toString()));
                        }

                        JsonArray array = jsonElement.getAsJsonArray();
                        // validate array items
                        for(JsonElement element : array) {
                            GroupInfo.validateJsonElement(element);
                        }
                        actualAdapter = adapterListGroupInfo;
                        match++;
                        log.log(Level.FINER, "Input data matches schema 'List<GroupInfo>'");
                    } catch (Exception e) {
                        // deserialization failed, continue
                        errorMessages.add(String.format(java.util.Locale.ROOT, "Deserialization for List<GroupInfo> failed with `%s`.", e.getMessage()));
                        log.log(Level.FINER, "Input data does not match schema 'List<GroupInfo>'", e);
                    }

                    if (match == 1) {
                        GetGroupsDefaultResponse ret = new GetGroupsDefaultResponse();
                        ret.setActualInstance(actualAdapter.fromJsonTree(jsonElement));
                        return ret;
                    }

                    throw new IOException(String.format(java.util.Locale.ROOT, "Failed deserialization for GetGroupsDefaultResponse: %d classes match result, expected 1. Detailed failure message for oneOf schemas: %s. JSON: %s", match, errorMessages, jsonElement.toString()));
                }
            }.nullSafe();
        }
    }

    // store a list of schema names defined in oneOf
    public static final Map<String, Class<?>> schemas = new HashMap<String, Class<?>>();

    public GetGroupsDefaultResponse() {
        super("oneOf", Boolean.FALSE);
    }

    public GetGroupsDefaultResponse(Object o) {
        super("oneOf", Boolean.FALSE);
        setActualInstance(o);
    }

    static {
        schemas.put("Map<String, GroupInfo>", Map.class);
        schemas.put("List<GroupInfo>", List.class);
    }

    @Override
    public Map<String, Class<?>> getSchemas() {
        return GetGroupsDefaultResponse.schemas;
    }

    /**
     * Set the instance that matches the oneOf child schema, check
     * the instance parameter is valid against the oneOf child schemas:
     * List&lt;GroupInfo&gt;, Map&lt;String, GroupInfo&gt;
     *
     * It could be an instance of the 'oneOf' schemas.
     */
    @Override
    public void setActualInstance(Object instance) {
        if (instance instanceof Map<?, ?>) {
            super.setActualInstance(instance);
            return;
        }

        if (instance instanceof List<?>) {
            List<?> list = (List<?>) instance;
            if (!list.isEmpty() && list.get(0) instanceof GroupInfo) {
                super.setActualInstance(instance);
                return;
            }
        }

        throw new RuntimeException("Invalid instance type. Must be List<GroupInfo>, Map<String, GroupInfo>");
    }

    /**
     * Get the actual instance, which can be the following:
     * List&lt;GroupInfo&gt;, Map&lt;String, GroupInfo&gt;
     *
     * @return The actual instance (List&lt;GroupInfo&gt;, Map&lt;String, GroupInfo&gt;)
     */
    @SuppressWarnings("unchecked")
    @Override
    public Object getActualInstance() {
        return super.getActualInstance();
    }

    /**
     * Get the actual instance of `Map&lt;String, GroupInfo&gt;`. If the actual instance is not `Map&lt;String, GroupInfo&gt;`,
     * the ClassCastException will be thrown.
     *
     * @return The actual instance of `Map&lt;String, GroupInfo&gt;`
     * @throws ClassCastException if the instance is not `Map&lt;String, GroupInfo&gt;`
     */
    @SuppressWarnings("unchecked")
    public Map<String, GroupInfo> getMapStringGroupInfo() throws ClassCastException {
        return (Map<String, GroupInfo>)super.getActualInstance();
    }

    /**
     * Get the actual instance of `List&lt;GroupInfo&gt;`. If the actual instance is not `List&lt;GroupInfo&gt;`,
     * the ClassCastException will be thrown.
     *
     * @return The actual instance of `List&lt;GroupInfo&gt;`
     * @throws ClassCastException if the instance is not `List&lt;GroupInfo&gt;`
     */
    @SuppressWarnings("unchecked")
    public List<GroupInfo> getListGroupInfo() throws ClassCastException {
        return (List<GroupInfo>)super.getActualInstance();
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement JSON Element
     * @throws IOException if the JSON Element is invalid with respect to GetGroupsDefaultResponse
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        // validate oneOf schemas one by one
        int validCount = 0;
        ArrayList<String> errorMessages = new ArrayList<>();
        // validate the json string with Map<String, GroupInfo>
        try {
            if (!jsonElement.isJsonObject()) {
                throw new IllegalArgumentException(String.format(java.util.Locale.ROOT, "Expected json element to be a object type in the JSON string but got `%s`", jsonElement.toString()));
            }

            validCount++;
        } catch (Exception e) {
            errorMessages.add(String.format(java.util.Locale.ROOT, "Deserialization for Map<String, GroupInfo> failed with `%s`.", e.getMessage()));
            // continue to the next one
        }
        // validate the json string with List<GroupInfo>
        try {
            if (!jsonElement.isJsonArray()) {
                throw new IllegalArgumentException(String.format(java.util.Locale.ROOT, "Expected json element to be a array type in the JSON string but got `%s`", jsonElement.toString()));
            }
            JsonArray array = jsonElement.getAsJsonArray();
            // validate array items
            for(JsonElement element : array) {
                GroupInfo.validateJsonElement(element);
            }
            validCount++;
        } catch (Exception e) {
            errorMessages.add(String.format(java.util.Locale.ROOT, "Deserialization for List<GroupInfo> failed with `%s`.", e.getMessage()));
            // continue to the next one
        }
        if (validCount != 1) {
            throw new IOException(String.format(java.util.Locale.ROOT, "The JSON string is invalid for GetGroupsDefaultResponse with oneOf schemas: List<GroupInfo>, Map<String, GroupInfo>. %d class(es) match the result, expected 1. Detailed failure message for oneOf schemas: %s. JSON: %s", validCount, errorMessages, jsonElement.toString()));
        }
    }

    /**
     * Create an instance of GetGroupsDefaultResponse given an JSON string
     *
     * @param jsonString JSON string
     * @return An instance of GetGroupsDefaultResponse
     * @throws IOException if the JSON string is invalid with respect to GetGroupsDefaultResponse
     */
    public static GetGroupsDefaultResponse fromJson(String jsonString) throws IOException {
        return JSON.getGson().fromJson(jsonString, GetGroupsDefaultResponse.class);
    }

    /**
     * Convert an instance of GetGroupsDefaultResponse to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}

