/*
 * Gerrit Code Review REST API
 * The Gerrit Code Review REST API. Paths, parameters, and payload schemas are generated statically from the server's REST bindings; human-authored summaries and descriptions are supplied here and merged in at build time.
 *
 * The version of the OpenAPI document: 3.15.0-SNAPSHOT
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package com.google.gerrit.client.model;

import java.util.Objects;
import com.google.gerrit.client.model.ProjectInfo;
import java.util.List;
import java.util.Map;



import java.io.IOException;
import java.lang.reflect.Type;
import java.util.logging.Level;
import java.util.logging.Logger;
import java.util.ArrayList;
import java.util.Collections;
import java.util.HashSet;
import java.util.HashMap;
import java.util.List;
import java.util.Map;

import com.google.gson.Gson;
import com.google.gson.GsonBuilder;
import com.google.gson.JsonParseException;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.reflect.TypeToken;
import com.google.gson.JsonPrimitive;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import com.google.gson.JsonDeserializationContext;
import com.google.gson.JsonDeserializer;
import com.google.gson.JsonSerializationContext;
import com.google.gson.JsonSerializer;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.JsonArray;
import com.google.gson.JsonParseException;

import com.google.gerrit.client.JSON;

@javax.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen", comments = "Generator version: 7.25.0")
public class GetProjectsDefaultResponse extends AbstractOpenApiSchema {
    private static final Logger log = Logger.getLogger(GetProjectsDefaultResponse.class.getName());

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!GetProjectsDefaultResponse.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'GetProjectsDefaultResponse' and its subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final Type typeInstanceMapStringProjectInfo = new TypeToken<Map<String, ProjectInfo>>(){}.getType();
            final TypeAdapter<Map<String, ProjectInfo>> adapterMapStringProjectInfo = (TypeAdapter<Map<String, ProjectInfo>>) gson.getDelegateAdapter(this, TypeToken.get(typeInstanceMapStringProjectInfo));

            final Type typeInstanceListProjectInfo = new TypeToken<List<ProjectInfo>>(){}.getType();
            final TypeAdapter<List<ProjectInfo>> adapterListProjectInfo = (TypeAdapter<List<ProjectInfo>>) gson.getDelegateAdapter(this, TypeToken.get(typeInstanceListProjectInfo));

            return (TypeAdapter<T>) new TypeAdapter<GetProjectsDefaultResponse>() {
                @Override
                public void write(JsonWriter out, GetProjectsDefaultResponse value) throws IOException {
                    if (value == null || value.getActualInstance() == null) {
                        elementAdapter.write(out, null);
                        return;
                    }

                    // check if the actual instance is of the type `Map<String, ProjectInfo>`
                    if (value.getActualInstance() instanceof Map<?, ?>) {
                    }
                    // check if the actual instance is of the type `List<ProjectInfo>`
                    if (value.getActualInstance() instanceof List<?>) {
                        List<?> list = (List<?>) value.getActualInstance();
                        if (!list.isEmpty() && list.get(0) instanceof ProjectInfo) {
                            JsonArray array = adapterListProjectInfo.toJsonTree((List<ProjectInfo>)value.getActualInstance()).getAsJsonArray();
                            elementAdapter.write(out, array);
                            return;
                        }
                    }
                    throw new IOException("Failed to serialize as the type doesn't match oneOf schemas: List<ProjectInfo>, Map<String, ProjectInfo>");
                }

                @Override
                public GetProjectsDefaultResponse read(JsonReader in) throws IOException {
                    Object deserialized = null;
                    JsonElement jsonElement = elementAdapter.read(in);

                    int match = 0;
                    ArrayList<String> errorMessages = new ArrayList<>();
                    TypeAdapter actualAdapter = elementAdapter;

                    // deserialize Map<String, ProjectInfo>
                    try {
                        // validate the JSON object to see if any exception is thrown
                        if (!jsonElement.isJsonObject()) {
                            throw new IllegalArgumentException(String.format(java.util.Locale.ROOT, "Expected json element to be a object type in the JSON string but got `%s`", jsonElement.toString()));
                        }

                        actualAdapter = adapterMapStringProjectInfo;
                        match++;
                        log.log(Level.FINER, "Input data matches schema 'Map<String, ProjectInfo>'");
                    } catch (Exception e) {
                        // deserialization failed, continue
                        errorMessages.add(String.format(java.util.Locale.ROOT, "Deserialization for Map<String, ProjectInfo> failed with `%s`.", e.getMessage()));
                        log.log(Level.FINER, "Input data does not match schema 'Map<String, ProjectInfo>'", e);
                    }
                    // deserialize List<ProjectInfo>
                    try {
                        // validate the JSON object to see if any exception is thrown
                        if (!jsonElement.isJsonArray()) {
                            throw new IllegalArgumentException(String.format(java.util.Locale.ROOT, "Expected json element to be a array type in the JSON string but got `%s`", jsonElement.toString()));
                        }

                        JsonArray array = jsonElement.getAsJsonArray();
                        // validate array items
                        for(JsonElement element : array) {
                            ProjectInfo.validateJsonElement(element);
                        }
                        actualAdapter = adapterListProjectInfo;
                        match++;
                        log.log(Level.FINER, "Input data matches schema 'List<ProjectInfo>'");
                    } catch (Exception e) {
                        // deserialization failed, continue
                        errorMessages.add(String.format(java.util.Locale.ROOT, "Deserialization for List<ProjectInfo> failed with `%s`.", e.getMessage()));
                        log.log(Level.FINER, "Input data does not match schema 'List<ProjectInfo>'", e);
                    }

                    if (match == 1) {
                        GetProjectsDefaultResponse ret = new GetProjectsDefaultResponse();
                        ret.setActualInstance(actualAdapter.fromJsonTree(jsonElement));
                        return ret;
                    }

                    throw new IOException(String.format(java.util.Locale.ROOT, "Failed deserialization for GetProjectsDefaultResponse: %d classes match result, expected 1. Detailed failure message for oneOf schemas: %s. JSON: %s", match, errorMessages, jsonElement.toString()));
                }
            }.nullSafe();
        }
    }

    // store a list of schema names defined in oneOf
    public static final Map<String, Class<?>> schemas = new HashMap<String, Class<?>>();

    public GetProjectsDefaultResponse() {
        super("oneOf", Boolean.FALSE);
    }

    public GetProjectsDefaultResponse(Object o) {
        super("oneOf", Boolean.FALSE);
        setActualInstance(o);
    }

    static {
        schemas.put("Map<String, ProjectInfo>", Map.class);
        schemas.put("List<ProjectInfo>", List.class);
    }

    @Override
    public Map<String, Class<?>> getSchemas() {
        return GetProjectsDefaultResponse.schemas;
    }

    /**
     * Set the instance that matches the oneOf child schema, check
     * the instance parameter is valid against the oneOf child schemas:
     * List&lt;ProjectInfo&gt;, Map&lt;String, ProjectInfo&gt;
     *
     * It could be an instance of the 'oneOf' schemas.
     */
    @Override
    public void setActualInstance(Object instance) {
        if (instance instanceof Map<?, ?>) {
            super.setActualInstance(instance);
            return;
        }

        if (instance instanceof List<?>) {
            List<?> list = (List<?>) instance;
            if (!list.isEmpty() && list.get(0) instanceof ProjectInfo) {
                super.setActualInstance(instance);
                return;
            }
        }

        throw new RuntimeException("Invalid instance type. Must be List<ProjectInfo>, Map<String, ProjectInfo>");
    }

    /**
     * Get the actual instance, which can be the following:
     * List&lt;ProjectInfo&gt;, Map&lt;String, ProjectInfo&gt;
     *
     * @return The actual instance (List&lt;ProjectInfo&gt;, Map&lt;String, ProjectInfo&gt;)
     */
    @SuppressWarnings("unchecked")
    @Override
    public Object getActualInstance() {
        return super.getActualInstance();
    }

    /**
     * Get the actual instance of `Map&lt;String, ProjectInfo&gt;`. If the actual instance is not `Map&lt;String, ProjectInfo&gt;`,
     * the ClassCastException will be thrown.
     *
     * @return The actual instance of `Map&lt;String, ProjectInfo&gt;`
     * @throws ClassCastException if the instance is not `Map&lt;String, ProjectInfo&gt;`
     */
    @SuppressWarnings("unchecked")
    public Map<String, ProjectInfo> getMapStringProjectInfo() throws ClassCastException {
        return (Map<String, ProjectInfo>)super.getActualInstance();
    }

    /**
     * Get the actual instance of `List&lt;ProjectInfo&gt;`. If the actual instance is not `List&lt;ProjectInfo&gt;`,
     * the ClassCastException will be thrown.
     *
     * @return The actual instance of `List&lt;ProjectInfo&gt;`
     * @throws ClassCastException if the instance is not `List&lt;ProjectInfo&gt;`
     */
    @SuppressWarnings("unchecked")
    public List<ProjectInfo> getListProjectInfo() throws ClassCastException {
        return (List<ProjectInfo>)super.getActualInstance();
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement JSON Element
     * @throws IOException if the JSON Element is invalid with respect to GetProjectsDefaultResponse
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        // validate oneOf schemas one by one
        int validCount = 0;
        ArrayList<String> errorMessages = new ArrayList<>();
        // validate the json string with Map<String, ProjectInfo>
        try {
            if (!jsonElement.isJsonObject()) {
                throw new IllegalArgumentException(String.format(java.util.Locale.ROOT, "Expected json element to be a object type in the JSON string but got `%s`", jsonElement.toString()));
            }

            validCount++;
        } catch (Exception e) {
            errorMessages.add(String.format(java.util.Locale.ROOT, "Deserialization for Map<String, ProjectInfo> failed with `%s`.", e.getMessage()));
            // continue to the next one
        }
        // validate the json string with List<ProjectInfo>
        try {
            if (!jsonElement.isJsonArray()) {
                throw new IllegalArgumentException(String.format(java.util.Locale.ROOT, "Expected json element to be a array type in the JSON string but got `%s`", jsonElement.toString()));
            }
            JsonArray array = jsonElement.getAsJsonArray();
            // validate array items
            for(JsonElement element : array) {
                ProjectInfo.validateJsonElement(element);
            }
            validCount++;
        } catch (Exception e) {
            errorMessages.add(String.format(java.util.Locale.ROOT, "Deserialization for List<ProjectInfo> failed with `%s`.", e.getMessage()));
            // continue to the next one
        }
        if (validCount != 1) {
            throw new IOException(String.format(java.util.Locale.ROOT, "The JSON string is invalid for GetProjectsDefaultResponse with oneOf schemas: List<ProjectInfo>, Map<String, ProjectInfo>. %d class(es) match the result, expected 1. Detailed failure message for oneOf schemas: %s. JSON: %s", validCount, errorMessages, jsonElement.toString()));
        }
    }

    /**
     * Create an instance of GetProjectsDefaultResponse given an JSON string
     *
     * @param jsonString JSON string
     * @return An instance of GetProjectsDefaultResponse
     * @throws IOException if the JSON string is invalid with respect to GetProjectsDefaultResponse
     */
    public static GetProjectsDefaultResponse fromJson(String jsonString) throws IOException {
        return JSON.getGson().fromJson(jsonString, GetProjectsDefaultResponse.class);
    }

    /**
     * Convert an instance of GetProjectsDefaultResponse to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}

